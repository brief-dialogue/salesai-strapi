'use strict';

/**
 * subjective-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subjective-question.subjective-question');
